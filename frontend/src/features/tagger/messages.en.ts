// English UI strings for the tagger slice. Edit directly; keys missing here fall
// back to the Hebrew slice via msg(), so partial translations are safe.

import type { taggerMessages } from "./messages";

export const taggerMessagesEn: Partial<Record<keyof typeof taggerMessages, string>> = {};
