// English UI strings for the explore slice. Edit directly; keys missing here fall
// back to the Hebrew slice via msg(), so partial translations are safe.

import type { exploreMessages } from "./messages";

export const exploreMessagesEn: Partial<Record<keyof typeof exploreMessages, string>> = {};
