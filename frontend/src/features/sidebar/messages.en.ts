// English UI strings for the sidebar slice. Edit directly; keys missing here fall
// back to the Hebrew slice via msg(), so partial translations are safe.

import type { sidebarMessages } from "./messages";

export const sidebarMessagesEn: Partial<Record<keyof typeof sidebarMessages, string>> = {};
