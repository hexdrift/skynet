// English UI strings for the agent-panel slice. Edit directly; keys missing here fall
// back to the Hebrew slice via msg(), so partial translations are safe.

import type { agentPanelMessages } from "./messages";

export const agentPanelMessagesEn: Partial<Record<keyof typeof agentPanelMessages, string>> = {};
