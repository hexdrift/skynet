// English UI strings for the datasets slice. Edit directly; keys missing here fall
// back to the Hebrew slice via msg(), so partial translations are safe.

import type { datasetsMessages } from "./messages";

export const datasetsMessagesEn: Partial<Record<keyof typeof datasetsMessages, string>> = {};
