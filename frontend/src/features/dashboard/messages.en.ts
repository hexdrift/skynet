// English UI strings for the dashboard slice. Edit directly; keys missing here fall
// back to the Hebrew slice via msg(), so partial translations are safe.

import type { dashboardMessages } from "./messages";

export const dashboardMessagesEn: Partial<Record<keyof typeof dashboardMessages, string>> = {};
