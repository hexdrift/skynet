// English UI strings for the submit slice. Edit directly; keys missing here fall
// back to the Hebrew slice via msg(), so partial translations are safe.

import type { submitMessages } from "./messages";

export const submitMessagesEn: Partial<Record<keyof typeof submitMessages, string>> = {};
