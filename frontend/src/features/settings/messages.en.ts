// English UI strings for the settings slice. Edit directly; keys missing here fall
// back to the Hebrew slice via msg(), so partial translations are safe.

import type { settingsMessages } from "./messages";

export const settingsMessagesEn: Partial<Record<keyof typeof settingsMessages, string>> = {};
