// English UI strings for the storage slice. Edit directly; keys missing here fall
// back to the Hebrew slice via msg(), so partial translations are safe.

import type { storageMessages } from "./messages";

export const storageMessagesEn: Partial<Record<keyof typeof storageMessages, string>> = {};
