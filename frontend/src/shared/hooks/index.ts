/**
 * Shared hooks index
 * Re-exports all shared hooks for consistent imports across features
 */

export { useApiCall } from "./use-api-call";
export { useDebounce } from "./use-debounce";
export { useLocalStorage } from "./use-local-storage";
